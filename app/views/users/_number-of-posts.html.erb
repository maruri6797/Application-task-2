<h2>投稿数の前日比・前週比</h2>
  <table class='table table-bordered'>
    <thead>
      <tr>
        <th>今日の投稿数</th>
        <th>前日の投稿数</th>
        <th>前日比</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= @books.created_today.count %></td>
        <td><%= @books.created_yesterday.count %></td>
        <td>
          <%= ((@books.created_today.count.to_f / @books.created_yesterday.count) * 100).round %>%
        </td>
      </tr>
    </tbody>
    </table>
    <table class="table table-bordered">
    <thead>
      <tr>
        <th>今週の投稿数</th>
        <th>前週の投稿数</th>
        <th>前週比</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= @books.created_this_week.count %></td>
        <td><%= @books.created_last_week.count %></td>
        <td>
          <%= ((@books.created_this_week.count.to_f / @books.created_last_week.count) * 100).round %>%
        </td>
      </tr>
        </tbody>
      </table>